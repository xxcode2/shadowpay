# Solana RPC URL
# ⚠️  Privacy Cash ONLY works on MAINNET (not devnet)
# Use Helius mainnet for fast RPC:
SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_API_KEY

# Or use devnet Helius (but Privacy Cash won't work):
# SOLANA_RPC_URL=https://devnet.helius-rpc.com/?api-key=YOUR_API_KEY

# Relayer Authentication Secret (MUST match backend)
RELAYER_AUTH_SECRET=shadowpay-relayer-secret-123

# Relayer Keypair Path
RELAYER_KEYPAIR_PATH=./relayer.json

# Port (Railway will override)
PORT=4444
